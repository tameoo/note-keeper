import React from 'react';
import NoteMenu from '../note-menu';

const HomePage = () => {
    return (
        <section id="home-page">
            <NoteMenu/>
        </section>
    );
}

export default HomePage;